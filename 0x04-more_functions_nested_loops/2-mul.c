#include "main.h"


/**
 * mul - Multiplies two integers
 * @x: The firs number to multiply
 * @y: The second number to multiply with second number
 * Return: Return the sum of multiplication between x param and y param
 */

int mul(int x, int y)
{
	return (x * y);
}
