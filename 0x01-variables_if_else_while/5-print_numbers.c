#include <stdio.h>

/**
 * main - print all strings from 0 to 9
 * Return: Always 0
 */
int main(void)
{
	int num;

	for (num = o; num < 10; num++)
		printf("%d", num);

	printf("\n");

		return (0);
}
